The following are what each script in the directory of this repo does:
1. libdynamic.so: it is a dynamic library created from various functions;
2. 1-create_dynamic_lib.sh: a script that creates a dynamic library from all .c files in the current directory;

