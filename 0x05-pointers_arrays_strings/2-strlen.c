#include "main.h"
#include <stdio.h>

/**
 * _strlen - prints the length of a string
 * @s: any character passed as an argument
 * Return: the lenght of the string in int
 */

int _strlen(char *s)
{
	strlen(*s);
}
