#include <stdio.h>
/**
 * main - prints string
 * Description: prints out a string to std output
 *
 * Return: value of zero
 */
int main(void)
{
	char str[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", str);

	return (0);
}
