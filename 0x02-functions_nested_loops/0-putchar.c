#include <stdio.h>
#include "main.h"

/**
 * main - prints the string _putchar followed by a newline
 * Return: Always 0 (success)
 */

int main(void)
{
	char ch = "_putchar";
	putchar(ch);

	return (0);
}
