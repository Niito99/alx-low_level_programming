This dir in the repo is do more code on functions and nested loops
1. the first file "1-isdigit" contains code that checks if the parameter c is a digit or not; returns 1 if digit and 0 if not digit.
