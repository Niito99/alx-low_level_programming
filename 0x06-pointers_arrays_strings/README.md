This readme file contains more code on pointers arrays and strings
The following are what each task numbered n does
0. writes a function that concatenates two strings
1. writes a function that appends the second string to the first one(at n bytes); meaning not more than n bytes of the string n
